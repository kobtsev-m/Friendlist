version: 1
backend:
  phases:
    postBuild:
      commands:
        - cd amplify/backend/function/friendlist/src
        - npm install -g node-prune
        - node-prune
        - yarn migration:run
        - cd ../../../../../
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
